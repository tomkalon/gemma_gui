<div class="py-4">
    <h2 class="text-2xl pb-2 dark:text-white uppercase">Ustawienia</h2>
    <div class="mb-2">
        <span>Aktualnie wybrany profil ustawień:</span>
        <span class="p-1 rounded dark:bg-blue-450">{{ object.settings.name }}</span>
    </div>
    <div class="flex mt-6 gap-2">
        <button class="btn btn-blue pt-4">Zmień profil</button>
        <button class="btn btn-green pt-4">Dodaj nowy</button>
    </div>
    <div>{{ form(select_profile_form) }}</div>
{#    <div>#}
{#        {% for settings in settings_names %}#}
{#            {{ settings.name }}#}
{#        {% endfor %}#}
{#    </div>#}

</div>