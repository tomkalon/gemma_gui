<!DOCTYPE html>
<html xml:lang="pl" class="dark">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Greenee© App{% endblock %}</title>
        <link rel="icon" href="{{  asset('build/favicon/favicon.svg') }}" sizes="any" type="image/svg+xml">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="bg-amber-100 dark:bg-darker-600 min-h-screen flex flex-col">
        {% if block("nav", "nav.html.twig") is defined %}
            {{ block("nav", "nav.html.twig") }}
        {% endif %}
        {% block body %}{% endblock %}
        {% if block("footer", "footer.html.twig") is defined %}
            {{ block("footer", "footer.html.twig") }}
        {% endif %}
    </body>
</html>
